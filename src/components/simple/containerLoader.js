import Loader from "./loader"

const ContainerLoader = () => {
    return (
        <div className="w-full h-full flex-center flex items-center align-center justify-center">
            <Loader />
        </div>
    )
}
export default ContainerLoader;